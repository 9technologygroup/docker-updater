# Build output
/dup
/dup-agent
/dist/
*.exe
*.test

# Coverage and profiling
coverage.out
coverage.html
*.prof
*.pprof

# Secrets and generated material. dup writes certs and tokens next to its
# config, and a stray `dup cert` in the working tree must never be committable.
*.pem
*.key
*.crt
*.token
*.secret
config.yml
config.yaml
.env
.env.*

# Editors and OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# Local scratch
/tmp/
/scratch/
